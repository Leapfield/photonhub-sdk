"""PhotonHub client test package.

Keeping the tests importable as a package prevents their ``conftest.py`` from
colliding with the independent top-level validation suite when both suites are
collected in one pytest invocation.
"""
